# {{projectName}} (OpenClaw runtime)

AgentSkills-compliant skill. Default mode is `monitor`.

## Install

```bash
cp -r ./ ~/.openclaw/workspace/skills/{{projectName}}
cp .env.example .env   # set CETUS_POOL_ID
```

## Recipe

{{recipeUrl}}
