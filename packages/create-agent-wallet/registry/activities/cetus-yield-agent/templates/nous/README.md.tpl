# {{projectName}} (Hermes Agent / Nous runtime)

Cetus CLMM yield agent for Hermes. Default mode is `monitor`.

## Run

Install Hermes, `cp .env.example .env`, set `CETUS_POOL_ID`, register as a Hermes skill.

## Recipe

{{recipeUrl}}
